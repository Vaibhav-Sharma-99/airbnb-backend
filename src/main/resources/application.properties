spring.application.name=airBnbApp

#DB Configuration

spring.datasource.url=jdbc:postgresql://${DB_HOST_URL}:5432/${DB_NAME}
# postgres-spring-projects.c2l6ei4su6mz.us-east-1.rds.amazonaws.com:5432/projects_airbnb
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

#spring.datasource.url=jdbc:postgresql://<url i got from aws>
#spring.datasource.username=username
#spring.datasource.password=myUsername
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

#server.servlet.context-path=/api/v1

jwt.secretKey = ${JWT_secretKey}
#jwt.secretKey = ${JWT_secretKey}

#flyway configs
#spring.flyway.baseline-on-migrate=true     //use if some data is  present in ur db(for existing db)
spring.flyway.enabled=false

frontend.url = http://localhost:8080
gi = ${stripe_sKey}

#spring.ai.ollama.base-url=http://localhost:11434
#spring.ai.ollama.chat.options.model=qwen3:0.6b

spring.ai.openai.api-key=${API_KEY}
spring.ai.openai.chat.options.model: gpt-4o-mini
spring.ai.openai.embedding.options.model: text-embedding-3-small

#in prod this is false
spring.ai.vectorstore.pgvector.initialize-schema: true